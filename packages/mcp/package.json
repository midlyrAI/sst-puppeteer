{
  "name": "@sst-puppeteer/mcp",
  "version": "0.0.0",
  "description": "MCP server frontend for sst-puppeteer — exposes SSTSession over the Model Context Protocol.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "bin": {
    "sst-puppeteer-mcp": "./dist/bin.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src test",
    "lint:fix": "eslint src test --fix",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@sst-puppeteer/core": "workspace:*",
    "zod": "^4.0.0"
  },
  "devDependencies": {}
}
