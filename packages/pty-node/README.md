# @sst-puppeteer/pty-node

Node PTY adapter for [sst-puppeteer](https://github.com/sst-puppeteer/sst-puppeteer) — wraps `node-pty` into the `PtyAdapter` port from `@sst-puppeteer/core`.

**v0.1 status:** stub. The class exists with the correct interface; methods throw `NotImplementedError`. The `node-pty` runtime dependency is added when the real implementation lands.
