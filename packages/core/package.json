{
  "name": "@sst-puppeteer/core",
  "version": "0.0.0",
  "description": "Runtime-agnostic domain core for sst-puppeteer — SSTSession, event fusion, state machines.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "eslint src test",
    "lint:fix": "eslint src test --fix",
    "prepublishOnly": "pnpm build"
  }
}
