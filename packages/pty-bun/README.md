# @sst-puppeteer/pty-bun

Bun PTY adapter for [sst-puppeteer](https://github.com/sst-puppeteer/sst-puppeteer) — wraps `Bun.spawn` (PTY mode) into the `PtyAdapter` port from `@sst-puppeteer/core`.

**v0.1 status:** stub. The class exists with the correct interface; methods throw `NotImplementedError`.
